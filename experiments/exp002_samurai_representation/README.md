# exp002 — SAMURAI representation benchmark
Controlled variable: `robust_band_rgb` vs `pca_rgb`; all tracker settings and selected sequences must remain fixed. The runner writes metadata/per-sequence/aggregate artifacts once a mounted SAMURAI API binding is verified. No HOTC result has been run in this environment.
