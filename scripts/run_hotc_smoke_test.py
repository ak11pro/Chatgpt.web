"""Five-frame SAMURAI smoke test; forwards to the real OPE runner."""
import argparse, subprocess, sys
p=argparse.ArgumentParser();p.add_argument('--data-root',required=True);p.add_argument('--annotations',required=True);p.add_argument('--checkpoint',required=True);p.add_argument('--source-root',required=True);p.add_argument('--config',required=True);p.add_argument('--representation',required=True);p.add_argument('--sequence',required=True);p.add_argument('--frames',type=int,default=5);p.add_argument('--output',default='experiments/exp004_kaggle_verification/artifacts');p.add_argument('--extension',default='.npy');a=p.parse_args()
cmd=[sys.executable,'scripts/run_hotc_ope.py','--data-root',a.data_root,'--annotations',a.annotations,'--checkpoint',a.checkpoint,'--source-root',a.source_root,'--config',a.config,'--representation',a.representation,'--sequences',a.sequence,'--frames',str(a.frames),'--extension',a.extension,'--output',a.output]; raise SystemExit(subprocess.call(cmd))
